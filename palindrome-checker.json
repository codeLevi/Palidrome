{"index.js":"function palindrome(str) {\n  // Good luck!\n  var remRevStr = str.replace(/\\W/g, \"\").replace(/_/g, \"\").toLowerCase().split(\"\").reverse();\n  console.log(remRevStr);\n  var joinRevStr = remRevStr.join(\"\");\n  console.log(joinRevStr);\n  \n  var remStr = str.replace(/\\W/g, \"\").replace(/_/g, \"\").toLowerCase().split(\"\");\n  console.log(remStr);\n  var joinStr = remStr.join(\"\");\n  console.log(joinStr);\n  \n  if(joinRevStr === joinStr){\n  return true \n  } else return false\n}\n\n\n\npalindrome(\"My age is 0, 0 si ega ym.\");"}